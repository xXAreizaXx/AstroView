// React & React Native
import { StyleSheet } from "react-native";

// Styles
export const styles = StyleSheet.create({
    button: {
        width: "100%",
    },
    container: {
        alignItems: "center",
        gap: 12,
        justifyContent: "center",
    },
    image: {
        width: 200,
        height: 200,
    },
});